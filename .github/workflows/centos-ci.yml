name: Centos CI

on: 
  push:
    branches:
      - release
      - 2.3
      - filesystem
jobs:
  build:
    runs-on: ubuntu-latest
    container: 'docker.io/kungfutrader/kungfu-builder-centos:latest'
    steps:
    - uses: actions/checkout@v2
    - name: build
      run: |
        yarn env-pypi
        yarn install
        yarn build
    - uses: actions/upload-artifact@v2
      with:
          path: app/build/app/*.rpm
