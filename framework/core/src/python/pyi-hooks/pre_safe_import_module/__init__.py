# ***************************************************************************
# ``pre_safe_import_module/`` |docname| - make this directory a dummy package
# ***************************************************************************
#
# The ``pre_safe_import_module/`` directory actually is not a package.
# But this dummy ``__init__.py`` file here eases packaging by setuptools,
# since one does not need to add the extra files.
