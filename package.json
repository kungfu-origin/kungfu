{
  "name": "kungfu",
  "author": "taurus.ai",
  "version": "2.1.0",
  "description": "KungFu Trader",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": ["core", "cli", "app"],
  "scripts": {
    "postinstall": "yarn wsrun --serial install-deps",
    "build": "npm config set kungfu-core:runtime electron && yarn wsrun --serial --fast-exit build",
    "clean": "yarn wsrun clean"
  },
  "dependencies": {
    "wsrun": "^5.0.0",
    "nan": "0.3.0"
  }
}
