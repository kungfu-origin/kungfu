{
  "name": "kungfu",
  "author": "taurus.ai",
  "version": "2.1.2",
  "description": "KungFu Trader",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": ["core", "app"],
  "scripts": {
    "postinstall": "yarn wsrun --serial install-deps && npm config set kungfu-core:runtime electron",
    "build": "yarn wsrun --serial --fast-exit build",
    "clean": "yarn wsrun clean"
  },
  "dependencies": {
    "wsrun": "^5.0.0",
    "nan": "0.3.0"
  }
}
