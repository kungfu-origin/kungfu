{
  "name": "kungfu",
  "author": "kungfu-origin",
  "description": "Kungfu Workspaces",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "framework/*",
    "developer/*",
    "extensions/*"
  ],
  "scripts": {
    "foreach": "wsrun --serial --exclude-missing --fast-exit",
    "postinstall": "yarn -s foreach postinstall",
    "build": "yarn -s foreach build",
    "build-cli": "yarn -s foreach build-cli",
    "clean": "yarn -s foreach clean",
    "package": "yarn -s foreach package",
    "rebuild": "yarn run clean && yarn run build"
  },
  "dependencies": {
    "@mapbox/node-pre-gyp": "^1.0.0",
    "cross-env": "1.0.0",
    "cross-var": "1.0.0",
    "node-gyp": "^7.1.0"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "wsrun": "^5.0.0"
  }
}
