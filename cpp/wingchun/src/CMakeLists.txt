AUX_SOURCE_DIRECTORY(util SOURCE_FILES_UTIL)
AUX_SOURCE_DIRECTORY(calendar SOURCE_FILES_CALENDAR)
AUX_SOURCE_DIRECTORY(event_loop SOURCE_FILES_EVENT_LOOP)
AUX_SOURCE_DIRECTORY(storage SOURCE_FILES_STORAGE)
AUX_SOURCE_DIRECTORY(gateway SOURCE_FILES_GATEWAY)
AUX_SOURCE_DIRECTORY(oms SOURCE_FILES_OMS)
AUX_SOURCE_DIRECTORY(commission SOURCE_FILES_COMMISSION)
AUX_SOURCE_DIRECTORY(portfolio SOURCE_FILES_PORTFOLIO)
AUX_SOURCE_DIRECTORY(strategy SOURCE_FILES_STRATEGY)

ADD_LIBRARY(${PROJECT_NAME} SHARED
        ${SOURCE_FILES_UTIL}
        ${SOURCE_FILES_CALENDAR}
        ${SOURCE_FILES_EVENT_LOOP}
        ${SOURCE_FILES_STORAGE}
        ${SOURCE_FILES_GATEWAY}
        ${SOURCE_FILES_OMS}
        ${SOURCE_FILES_COMMISSION}
        ${SOURCE_FILES_PORTFOLIO}
        ${SOURCE_FILES_STRATEGY}
        )

SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES LINKER_LANGUAGE CXX)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} yijinjing ${Boost_LIBRARIES})
